version: '3.8'

services:
  sheetvault:
    image: ${IMAGE_TAG:-sheetvault:latest}
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/app/data/sheetvault.db
    restart: unless-stopped
